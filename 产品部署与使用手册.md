# SpeakMate 产品部署与使用手册

## 1. 产品概述

SpeakMate 是一个**英语口语陪练 AI Agent**，核心能力：

- **按场景对话**：6 个手工设计的生活场景（机场值机 / 酒店入住 / 餐厅点餐 / 求职面试 / 就医问诊 / 购物退货），每个场景含背景设定、NPC 角色与难度分级（难度按**词汇量与句型复杂度**划分 A1–C1 五档）。
- **实时语法纠错**：24 条确定性语法规则（主谓一致、时态搭配、冠词、情态动词等）先做规则匹配，再交给 LLM 做深度分析；纠错结果同类合并、按频率排序，每轮对话结束输出该轮错误报告。
- **难度随水平自适应**：动态难度调节子 Agent 按答题正确率调整词汇与句型难度。
- **进阶能力**：RAG 检索对应级别的语法知识库并在纠错时给出规则出处；长期记忆追踪常犯错误类型并生成针对性练习。

技术栈：FastAPI + SQLite + sherpa-onnx（离线语音识别）+ edge-tts（语音合成）+ 8 个 LLM Agent（LangChain 风格编排）；前端 React 18 + Vite + TypeScript。

## 2. 系统架构

```
┌─────────────────────────── 前端 (React + Vite) ───────────────────────────┐
│  登录/引导 → 今日/场景库 → 练习页(PracticeRoom) → 成长/我的/设置           │
│  语音录音 → 消息气泡(含纠错感叹号) → 实时纠错侧栏 → 每轮报告              │
└───────────────┬───────────────────────────────────────────────────────────┘
                │ HTTP / SSE (http://localhost:8000/api/*)
┌───────────────▼───────────────────────────────────────────────────────────┐
│                           后端 (FastAPI)                                  │
│  routers/ 14 组 API ──► services/ 业务层                                  │
│     语音闭环: asr.py(离线识别) → enhanced_turn.py(增强回合) → tts.py       │
│     语法引擎: grammar_rules.py(24规则) → grammar_service.py(LLM深度分析)   │
│               → grammar_rag.py(级别知识库检索+出处) → error_aggregation.py │
│     学习闭环: learning_loop.py(复盘→记忆→计划调整)                          │
│     Agent层:  agents.py(8个Agent) / difficulty_agent.py(难度调节)          │
│     存储:     SQLite (coach.sqlite: 用户/计划/会话/纠错/记忆/复盘)          │
└───────────────────────────────────────────────────────────────────────────┘
```

## 3. 环境准备

| 依赖 | 版本要求 | 说明 |
|------|----------|------|
| Python | ≥ 3.11 | 后端运行环境 |
| 外网 | 可用 | 调用 LLM API（OpenRouter/DeepSeek 等）；不配置则运行于内置 fake 演示模式 |
| Node.js | ≥ 18（可选） | 仅重新构建前端时需要；本提交版已预编译 dist |

语音识别模型（sherpa-onnx whisper-tiny.en，约 100MB，共 3 个文件）**需自行下载**（国内推荐 hf-mirror 镜像，见 4.5 节），放入 `data/asr_model/sherpa-onnx-whisper-tiny.en/` 后自动启用；未下载时仅语音功能不可用，打字对话、纠错、报告等功能不受影响。

## 4. 安装部署

### 4.1 快速部署（单端口，推荐评委体验）

```bash
# 1. 安装依赖
cd "speaking practice 提交版"
pip install -r requirements.txt

# 2. 配置环境变量（可选，见第 5 节）
copy .env.example .env     # Windows；macOS/Linux 用 cp

# 3. 启动（在 app/backend 目录下）
cd app/backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

访问 **http://localhost:8000**。本提交版已对 `app/backend/app/main.py` 增加前端静态挂载（`app/frontend/dist`），页面与 API 同端口，无需单独启动前端。

> 注：源项目（开发仓库）未包含该静态挂载段；提交版为发布体验所做增强，代码内有注释说明。

### 4.2 开发模式（前后端分离）

```bash
# 终端 1：后端（8000）
cd app/backend && python -m uvicorn app.main:app --reload --port 8000

# 终端 2：前端（5173，已配置 CORS 允许）
cd app/frontend && npm install && npm run dev
```

前端默认请求 `http://localhost:8000`（见 `src/api.ts` 的 `API_BASE`），可通过 `VITE_API_BASE` 环境变量覆盖。

### 4.3 重新构建前端

```bash
cd app/frontend
npm install
npm run build        # 产物输出到 dist/，单端口模式自动生效
npm test             # 前端 36 个用例（vitest）
```

### 4.4 运行测试

```bash
cd app/backend
python -m pytest                 # 后端 126 个用例
python e2e_simulation.py         # 端到端全流程模拟（真实后端 + 学生 LLM 走完整对话并出报告）
```

### 4.5 下载语音识别模型（可选，启用语音对话）

语音识别使用 sherpa-onnx 的 whisper-tiny.en 离线模型（约 100MB，共 3 个文件）。**国内用户推荐 hf-mirror 镜像，速度快**；GitHub 官方源作为兜底。

**方式一：命令行（推荐，国内镜像）**

```powershell
cd data
mkdir asr_model\sherpa-onnx-whisper-tiny.en
cd asr_model\sherpa-onnx-whisper-tiny.en
curl -L -O https://hf-mirror.com/csukuangfj/sherpa-onnx-whisper-tiny.en/resolve/main/tiny.en-encoder.int8.onnx
curl -L -O https://hf-mirror.com/csukuangfj/sherpa-onnx-whisper-tiny.en/resolve/main/tiny.en-decoder.int8.onnx
curl -L -O https://hf-mirror.com/csukuangfj/sherpa-onnx-whisper-tiny.en/resolve/main/tiny.en-tokens.txt
```

**方式二：浏览器下载**：打开 https://hf-mirror.com/csukuangfj/sherpa-onnx-whisper-tiny.en → Files，下载上述 3 个文件放入 `data/asr_model/sherpa-onnx-whisper-tiny.en/`。

**方式三：GitHub 兜底**：官方打包 `https://github.com/k2-fsa/sherpa-onnx/releases/download/asr-models/sherpa-onnx-whisper-tiny.en.tar.bz2`，解压后把 `sherpa-onnx-whisper-tiny.en/` 目录整体放入 `data/asr_model/`。

**验证**：最终结构应为

```
data/asr_model/sherpa-onnx-whisper-tiny.en/
  ├── tiny.en-encoder.int8.onnx
  ├── tiny.en-decoder.int8.onnx
  └── tiny.en-tokens.txt
```

启动日志出现 `ASR 模型就绪` 或语音功能可正常使用即表示模型生效。

## 5. 配置说明

复制 `.env.example` 为 `.env` 后按需修改（**真实密钥只放 .env，不随提交版分发**）：

| 变量 | 默认 | 说明 |
|------|------|------|
| `COACH_DB_PATH` | `./data/coach.sqlite` | SQLite 路径（相对启动目录），首次启动自动建库建表 |
| `LLM_PROVIDER` | `fake` | `openrouter` / `deepseek` / `fake`（fake 为无 Key 演示模式） |
| `LLM_API_KEY` | 空 | LLM API 密钥 |
| `LLM_BASE_URL` | 空 | 如 `https://openrouter.ai/api/v1` |
| `PLANNER_MODEL` | `fake-local-coach` | 计划/复盘/记忆类 Agent 使用的模型 |
| `CHAT_MODEL` | `fake-local-coach` | 对话/纠错类 Agent 使用的模型 |
| `TTS_VOICE` | `en-US-JennyNeural` | 默认陪练音色（可设置页切换） |

## 6. 需求实现对照

| 需求 | 实现位置 |
|------|----------|
| ① ≥5 个手工场景（背景/角色/难度分级按词汇量与句型复杂度） | `app/backend/app/scenarios.py`（6 个场景，DifficultyBand） |
| ② ≥10 条语法规则，先规则后 LLM | `grammar_rules.py`（24 条）+ `grammar_service.py`（analyze 流程） |
| ③ 纠错汇总去重排序 | `error_aggregation.py`（同类合并 + 频率/严重度排序）+ `grammar_service.py:_deduplicate` |
| ④ 每轮对话结束输出该轮错误报告 | `services/enhanced_turn.py`（round_report 落库）+ `GET /api/sessions/{id}/learning-report` |
| ⑤ RAG 级别语法知识库 + 规则出处 | `grammar_rag.py`（分级知识库 + level-aware 检索 + knowledge_for_rule） |
| ⑥ 动态难度调节子 Agent（按正确率） | `difficulty_agent.py`（≥0.8 升档 / <0.6 降档） |
| ⑦ 长期记忆 + 针对性练习 | `long_term_memory.py` + `learning_loop.py`（复盘→记忆→计划调整闭环） |

完整代码位置清单见源项目文档《SpeakMate需求实现代码位置清单.txt》（桌面「解答」目录同步更新版）。

## 7. 常见问题

**Q1：启动报错 `ModuleNotFoundError: sherpa_onnx`？**
依赖未装全，执行 `pip install -r requirements.txt`；若安装 sherpa-onnx 失败，可先启动（语音功能会降级），对话功能不受影响。

**Q2：不配置 LLM Key 能跑吗？**
能。`LLM_PROVIDER=fake` 时使用内置演示逻辑，可完整体验场景对话、规则纠错、报告、记忆等流程；真实 LLM 仅增强"深度分析/复盘/计划"的生成质量。

**Q3：页面打开但接口 404？**
确认在 `app/backend` 目录下启动（数据库与静态路径按项目根解析）；页面与 API 同端口时浏览器直接访问 8000 即可。

**Q4：语音识别不准 / 加载慢？**
首次加载 ASR 模型需数秒；建议使用清晰麦克风。识别使用 `data/asr_model/sherpa-onnx-whisper-tiny.en`（whisper-tiny int8 版，离线）。若语音报错提示模型缺失，请先按 4.5 节下载模型（国内镜像 hf-mirror 更快）。

**Q5：如何查看学习报告 / 长期记忆？**
完成练习后查看"成长"页（复盘、教练记忆、自适应调整）；每轮纠错报告在练习页侧栏与消息气泡感叹号处查看。

**Q6：数据存哪里？**
SQLite：`app/backend/data/coach.sqlite`（默认）。如需重置，删除该文件后重启即可自动重建。

## 8. 安全说明

- 本提交版**不含任何真实密钥**：`.env` 未随包分发，仅提供 `.env.example` 模板；`.gitignore` 已忽略 `.env`。
- 默认 `JWT_SECRET` 为占位值，生产部署请在 `.env` 中设置强随机密钥。
- 依赖仅包含开源组件（FastAPI / sherpa-onnx / edge-tts 等），无第三方闭源 SDK。
