# ANALYSIS — 分析文档

项目:SpeakMate 英语口语陪练 AI Agent(提交版 v2)

## 1. 需求分析

### 1.1 问题域

英语口语学习者普遍面临:①缺少真实对话环境;②输出错误无人即时纠正;③反馈笼统(如"语法有问题"),不知道错在哪、为何错、怎么改;④练习内容与自身水平不匹配。

### 1.2 需求提炼

| 需求类别 | 核心诉求 | 本项目解法 |
|----------|----------|------------|
| 场景化 | 真实生活场景练习 | 6 个手工设计场景 + NPC 角色扮演 |
| 可解释纠错 | 知道错在哪、为什么 | 24 条确定性规则 + RAG 知识出处 |
| 水平匹配 | 难度随水平变化 | CEFR A1–C1 五档 + 正确率驱动的自适应 |
| 持续性 | 长期进步、不遗忘 | 长期记忆 + 学习闭环(复盘→记忆→调计划) |
| 低门槛 | 随时可用、开箱即用 | 游客入口 + 单端口发布 + 离线 ASR |

## 2. 技术选型分析

| 环节 | 选型 | 备选 | 选择理由 |
|------|------|------|----------|
| Web 框架 | FastAPI | Flask / Django | 异步 SSE 流式响应、Pydantic 校验、自动 OpenAPI 文档 |
| 存储 | SQLite | MySQL / PostgreSQL | 本地优先、零运维、单文件备份;单用户场景足够 |
| 前端 | React 18 + Vite + TS | Vue / 原生 | 组件化、生态成熟、Vite 构建快 |
| 语音识别 | sherpa-onnx(离线) | 云端 ASR | **离线可用**,隐私好、无延迟抖动;whisper-tiny 精度够用 |
| 语音合成 | edge-tts | pyttsx3 / 云 TTS | 免费、音色多(14 种)、自然度高 |
| LLM 接入 | LangChain 风格编排 + OpenRouter/DeepSeek | 单一模型直调 | 多 Agent 分工;provider 可切换;无 Key 可降级 fake 模式 |
| RAG | 内置分级知识库 + 词法检索 | 向量库(Chroma/FAISS) | 语料小且人工精选,词法检索足够;接口预留可替换向量库 |

### 2.1 关键权衡:先规则后 LLM

- **纯 LLM 纠错**的缺点:成本高、延迟大、幻觉风险、无法保证确定性规则命中。
- **纯规则纠错**的缺点:覆盖面窄,语义级错误无能为力。
- **先规则后 LLM**:24 条高频规则离线秒级命中(确定性、免费),未覆盖处由 LLM 深度分析补充;同类错误合并排序避免刷屏。兼顾**准确性、成本、可解释性**。

### 2.2 关键权衡:难度分级标准

难度不按"句子长度"这种伪指标,而按**词汇量区间 + 句型复杂度 + 目标语言功能**(DifficultyBand 三要素),与 CEFR 对齐,可解释、可测试。

## 3. 多 Agent 架构分析

8 个 Agent 各司其职,分工解耦:

| Agent | 职责 | 解决的问题 |
|-------|------|------------|
| GoalAgent | 学习目标 → 分阶段计划 | 目标拆解 |
| CoachOrchestratorAgent | 编排对话与练习节奏 | 多 Agent 协作 |
| ConversationAgent | 场景 NPC 对话 | 角色扮演真实性 |
| InlineFeedbackAgent | 行内即时反馈 | 反馈及时性 |
| LanguageSupportAgent | 母语提示/翻译支持 | 初学者卡壳 |
| DailyReviewAgent | 每日复盘 | 进步可视化 |
| MemoryAgent | 提取常犯错误(长期记忆) | 遗忘曲线 |
| PlanAdaptationAgent | 计划调整 | 动态适配 |

## 4. 需求覆盖分析

| 提交要求 | 覆盖情况 |
|----------|----------|
| 场景 ≥5 个,含背景/角色/难度分级 | ✅ 6 个场景,5 档难度 |
| 语法规则 ≥10 条,先规则后 LLM | ✅ 24 条规则 + LLM 深度分析 |
| 纠错汇总去重排序 | ✅ error_aggregation 同类合并 + 频率排序 |
| 每轮错误报告 | ✅ round_report 落库 + 前端「本轮总结」卡片 |
| RAG 知识库 + 规则出处 | ✅ grammar_rag 24 条 + level-aware 检索 |
| 动态难度调节 | ✅ difficulty_agent(≥80% 升 / <60% 降) |
| 长期记忆 + 针对性练习 | ✅ long_term_memory + learning_loop |
| 语音闭环 | ✅ sherpa-onnx 离线识别 + edge-tts 14 音色 |
| 测试覆盖 | ✅ 后端 126 用例 + 前端 vitest(见测试结果) |

详细需求实现对照见 REQUIREMENTS.md 第 5 节。

## 5. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 无 LLM API Key 时功能降级 | 深度分析质量下降 | fake 演示模式,规则纠错与全流程仍可用 |
| sherpa-onnx 安装失败 | 语音不可用 | 打字对话、纠错、报告不受影响 |
| ASR 识别不准确 | 口语练习体验 | 离线 whisper-tiny 已够用;支持打字兜底 |
| 知识库语料有限(24 条) | RAG 覆盖范围 | 接口预留替换向量库;来源附权威语法资料链接 |

## 6. 测试分析

- **后端**:126 个 pytest 用例,覆盖 auth(注册/登录/刷新)、API 契约、会话/计划/报告、语法引擎、RAG、难度调节、学习闭环、工具函数等。
- **前端**:vitest 用例覆盖核心组件逻辑(对话渲染、纠错卡片、状态流)。
- **E2E**:`e2e_simulation.py` 以真实后端 + 学生 LLM 走完整对话并产出报告,验证全流程。

最新测试执行结果见「测试用例 + 测试结果 / 测试结果」。
