# REQUIREMENTS — 需求文档

项目:SpeakMate 英语口语陪练 AI Agent(提交版 v2)

## 1. 项目背景与目标

英语学习者的口语输出缺少**即时、可解释、场景化**的反馈。传统练习要么是死记硬背,要么反馈滞后。本项目目标是构建一个 **AI 口语陪练 Agent**:

- 提供真实生活场景的角色扮演对话,降低开口门槛;
- 对学员表达做**实时语法纠错**,并解释"为什么错、怎么改";
- 难度随学员水平**自适应**调整;
- 通过长期记忆与学习闭环帮助学员**持续进步**。

## 2. 用户角色

| 角色 | 描述 | 核心诉求 |
|------|------|----------|
| 学员(游客/注册用户) | 英语学习者,水平 A1–C1 | 场景对话、即时纠错、看得见进步 |
| 教练(系统) | 多 Agent 编排的 AI 陪练 | 扮演 NPC、纠错、复盘、调计划 |

## 3. 功能需求

### 3.1 场景口语陪练

- FR-1:提供 **≥5 个**手工设计的生活场景。✅ 实现 6 个:机场值机、酒店入住、餐厅点餐、求职面试、就医问诊、购物退货。
- FR-2:每个场景包含背景设定、NPC 角色、学员角色与学习目标。
- FR-3:难度分级按**词汇量 + 句型复杂度**划分(CEFR A1–C1 五档),而非简单按句子长度。

### 3.2 实时语法纠错

- FR-4:**≥10 条**确定性语法规则,先规则匹配、后 LLM 深度分析。✅ 实现 24 条(主谓一致、时态、冠词、情态动词等)。
- FR-5:纠错结果**同类合并、按频率/严重度排序**,避免刷屏。
- FR-6:每条纠错给出「你说的是 → 建议改成 → 为什么 → 下次直接说」结构化反馈。

### 3.3 每轮错误报告

- FR-7:每轮对话结束输出该轮错误汇总(统计、类型标签、错误明细)。
- FR-8:可查看会话级累计学习报告。

### 3.4 RAG 语法知识库

- FR-9:内置**分级语法知识库**(RAG),纠错时按学员等级检索并给出**规则出处**。

### 3.5 难度自适应

- FR-10:动态难度调节子 Agent 按答题正确率调整:≥80% 升档,<60% 降档,词汇量与句型同步调整。

### 3.6 长期记忆与学习闭环

- FR-11:追踪学员**常犯错误类型**(长期记忆)。
- FR-12:生成针对性练习,学习闭环自动**复盘 → 记忆 → 计划调整**。

### 3.7 语音闭环

- FR-13:录音 → 离线 ASR 识别 → 纠错 → TTS 语音回复。
- FR-14:提供多种陪练音色(14 种美/英/澳音色)。

### 3.8 用户系统

- FR-15:手机号注册登录(JWT)、游客直接体验。
- FR-16:首次引导收集学习目标 / 每天时长 / 当前水平,生成分阶段学习计划。

## 4. 非功能需求

| 编号 | 需求 | 说明 |
|------|------|------|
| NFR-1 | 本地优先 | SQLite 存储 + 离线 ASR,核心功能不依赖外网 |
| NFR-2 | 可解释 | 纠错附规则出处与知识来源 |
| NFR-3 | 易部署 | 单端口发布(页面 + API 同端口),免 Node 环境 |
| NFR-4 | 可测试 | 后端 pytest、前端 vitest 全量覆盖核心逻辑 |
| NFR-5 | 安全性 | 密钥不入库不入包(仅 .env.example),JWT 鉴权,游客限流 |

## 5. 需求实现对照

| 需求 | 实现位置 |
|------|----------|
| ① ≥5 个手工场景(背景/角色/难度分级) | `app/backend/app/scenarios.py`(6 个场景,`DifficultyBand`) |
| ② ≥10 条语法规则,先规则后 LLM | `grammar_rules.py`(24 条)+ `grammar_service.py`(analyze 流程) |
| ③ 纠错汇总去重排序 | `error_aggregation.py`(同类合并 + 频率/严重度排序) |
| ④ 每轮错误报告 | `services/enhanced_turn.py`(round_report 落库)+ `GET /api/sessions/{id}/learning-report` |
| ⑤ RAG 分级知识库 + 规则出处 | `grammar_rag.py`(分级知识库 + level-aware 检索 + knowledge_for_rule) |
| ⑥ 动态难度调节子 Agent | `difficulty_agent.py`(≥0.8 升档 / <0.6 降档) |
| ⑦ 长期记忆 + 针对性练习 | `long_term_memory.py` + `learning_loop.py`(复盘→记忆→计划调整闭环) |
| ⑧ 语音闭环(ASR + TTS) | `asr.py`(sherpa-onnx 离线识别)+ `tts.py`/`tts_voices.py`(edge-tts 14 音色) |
| ⑨ 注册登录 + 游客 | `routers/auth.py` + `security.py`(JWT)+ 登录页「游客体验」入口 |
| ⑩ 首次引导与学习计划 | `routers/profiles.py` + `agents.py`(GoalAgent/CoachOrchestratorAgent) |

## 6. 验收标准

1. 在无 LLM API Key 的情况下,`fake` 演示模式可完整跑通:注册/游客 → 选场景 → 对话 → 纠错 → 每轮报告。
2. 输入 `I want to order a pizza and I am very hunger.`(餐厅场景),可观察到纠错卡片(主谓/拼写类)与消息气泡感叹号。
3. 后端 `pytest` 全量通过(126 用例);前端 `vitest` 通过(见「测试用例 + 测试结果」)。
4. 单端口启动后浏览器访问 `http://localhost:8000` 即可体验,无需 Node 环境。
